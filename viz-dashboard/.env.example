# Backend API URL
# For local development, the backend runs on port 8000
VITE_API_URL=http://localhost:8000

# Data source mode
# Options: 'live' (fetch from API), 'mock' (use generated mock data)
VITE_DATA_MODE=live
